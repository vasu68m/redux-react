import Home from "../components/Home";

export default Home;
// container helps to connects(map) components(react.js) to services(redux)
// in REDUX we use component indirectly via container instead of using directly
